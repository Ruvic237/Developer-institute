#Currencies

class Currency:
    def __init__(self,currency,amount):
        self.currency = currency
        self.amount = amount
        if () == str:
            print(f"{self.currency} {self.amount}")
        elif int() == int:
            print(f"{self.amount}")
        
    
c1 = Currency("dolar",5)
str(c1)
int(c1)
        
        
        